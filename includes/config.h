#pragma once

#include <string>

// 修改这个头文件使得可以在你的电脑上运行

// 项目根目录
const std::string rootPath = ".";

const std::string shaderPath = rootPath + "\\Shaders";
const std::string imagePath = rootPath + "\\images";
